using Infrastructure.Data;
using static Infrastructure.Data.AccessDBContext;

namespace Infrastructure.Interfaces
{
    public interface ITokenRepository
    {
        //TokenModel GetTokenPropertiesByToken(string token);
        //bool DisableTokenStatus(string token);
    }
}
